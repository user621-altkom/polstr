/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Stabilna część next-env.d.ts, wersjonowana w repo. Dzięki temu `npm run typecheck`
// działa na świeżym klonie, zanim `next dev` albo `next build` wygeneruje next-env.d.ts
// (ten plik jest w .gitignore, Next.js nadpisuje go przy każdym uruchomieniu).
